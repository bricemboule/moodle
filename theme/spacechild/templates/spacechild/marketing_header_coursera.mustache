<!-- ============================================
     PAGE AUDIENCE - HEADER UNIFIÉ
     Identique à la page d'accueil
     ============================================ -->

<div class="sc-landing sc-landing--full {{#hastoplinks}}has-topstrip{{/hastoplinks}}">

  <!-- Top Strip -->
  {{#hastoplinks}}
  <div class="sc-topbar">
    <div class="sc-container sc-topbar-inner">
      <a href="{{peopleurl}}">Pour les personnes</a>
      <a href="{{universitiesurl}}">Pour les universités</a>
      <a href="{{governmentsurl}}">Pour les gouvernements</a>
    </div>
  </div>
  {{/hastoplinks}}

  <!-- Navigation -->
  <header class="sc-nav">
    <div class="sc-container sc-nav-inner">
      <a class="sc-logo" href="{{wwwroot}}">
        <img class="sc-logo-image" src="{{wwwroot}}/theme/spacechild/images/LOGO.png" alt="{{sitename}}">
      </a>
      
      <a class="sc-nav-link" href="{{wwwroot}}/course/index.php">Explorer</a>
      
      <form class="sc-nav-search" action="{{wwwroot}}/course/search.php" method="get" role="search">
        <label class="sc-visually-hidden" for="sc-nav-search">Que souhaitez-vous apprendre ?</label>
        <input id="sc-nav-search" type="text" name="search" placeholder="Que souhaitez-vous apprendre ?" aria-label="Que souhaitez-vous apprendre ?">
        <button class="sc-search-btn" type="submit" aria-label="Rechercher">
          <svg class="sc-search-btn__icon" width="18" height="18" viewBox="0 0 18 18" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <circle cx="8" cy="8" r="6.5" stroke="currentColor" stroke-width="1.5"/>
            <path d="M12.5 12.5L16.5 16.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
          </svg>
        </button>
      </form>
      
      <nav class="sc-nav-actions" aria-label="Actions utilisateur">
        <a class="sc-link" href="{{{loginurl}}}" data-login-modal data-modal-title="Connexion">Connexion</a>
        <a class="sc-btn sc-btn--signup" href="{{{signupurl}}}" data-signup-modal data-modal-title="Inscription">S'inscrire gratuitement</a>
      </nav>
    </div>
  </header>

  <!-- Hero -->
  <section class="sc-hero" aria-labelledby="hero-title">
    <div class="sc-container">
      <div class="sc-hero-grid">
        <div class="sc-hero-copy">
          <span class="sc-eyebrow">{{page_title}}</span>
          <h1 id="hero-title">{{hero_title}}</h1>
          <p>{{hero_subtitle}}</p>
          <div class="sc-hero-actions">
            {{#cta_primary_label}}
            <a class="sc-btn sc-btn--primary" href="{{{cta_primary_url}}}">{{cta_primary_label}}</a>
            {{/cta_primary_label}}
            <a class="sc-btn sc-btn--ghost" href="{{{cta_secondary_url}}}">{{cta_secondary_label}}</a>
          </div>
        </div>
        {{#hero_image}}
        <div class="sc-hero-panel">
          <div class="sc-feature-card sc-animate">
            <div class="sc-card-media">
              <img src="{{hero_image}}" alt="{{hero_image_alt}}" loading="lazy">
            </div>
          </div>
        </div>
        {{/hero_image}}
      </div>
    </div>
  </section>

  <!-- Stats Hero -->
  {{#campus_stats}}
  <section class="sc-section sc-section--soft" aria-labelledby="stats-title">
    <div class="sc-container">
      <div class="sc-outcomes sc-stagger">
        {{#campus_stats}}
        <div class="sc-outcome">
          <div class="sc-outcome-value">{{value}}</div>
          <div class="sc-outcome-label">{{label}}</div>
        </div>
        {{/campus_stats}}
      </div>
    </div>
  </section>
  {{/campus_stats}}

  {{#government_stats}}
  <section class="sc-section sc-section--soft" aria-labelledby="stats-title">
    <div class="sc-container">
      <div class="sc-outcomes sc-stagger">
        {{#government_stats}}
        <div class="sc-outcome">
          <div class="sc-outcome-value">{{value}}</div>
          <div class="sc-outcome-label">{{label}}</div>
        </div>
        {{/government_stats}}
      </div>
    </div>
  </section>
  {{/government_stats}}

  <!-- Partenaires -->
  {{#partners}}
  <section class="sc-section" aria-labelledby="partners-title">
    <div class="sc-container">
      <h2 id="partners-title" style="text-align: center; margin-bottom: 2rem;">Nos partenaires</h2>
      <div class="sc-logos-row" style="display: flex; flex-wrap: wrap; gap: 2rem; justify-content: center; align-items: center;">
        {{#partners}}
        <span class="sc-logo-pill">{{name}}</span>
        {{/partners}}
      </div>
    </div>
  </section>
  {{/partners}}

  <!-- Goals -->
  {{#goals}}
  <section class="sc-section sc-section--alt" aria-labelledby="goals-title">
    <div class="sc-container">
      <div class="sc-section-head sc-section-head--stack">
        <h2 id="goals-title">Qu'est-ce qui vous amène ici aujourd'hui ?</h2>
        <p class="sc-section-sub">Choisissez votre objectif</p>
      </div>
      <div class="sc-grid sc-grid--features sc-stagger">
        {{#goals}}
        <div class="sc-card">
          <div style="font-size: 2.5rem; margin-bottom: 1rem;">{{icon}}</div>
          <h3>{{title}}</h3>
          <p>{{text}}</p>
        </div>
        {{/goals}}
      </div>
    </div>
  </section>
  {{/goals}}

  <!-- Learning Paths -->
  {{#learning_paths}}
  <section class="sc-section" aria-labelledby="learning-paths-title">
    <div class="sc-container">
      <div class="sc-section-head sc-section-head--stack">
        <h2 id="learning-paths-title">Ce que vous pouvez apprendre</h2>
        <p class="sc-section-sub">Explorez nos domaines de formation</p>
      </div>
      <div class="sc-grid sc-grid--programs sc-stagger">
        {{#learning_paths}}
        <a class="sc-card" href="{{{url}}}">
          <div style="font-size: 3rem; margin-bottom: 1rem;">{{icon}}</div>
          <h3>{{title}}</h3>
          <p>{{description}}</p>
          <div class="sc-card-meta" style="margin-top: auto; padding-top: 1rem; border-top: 1px solid var(--sc-border);">
            <small><strong>Cours populaires :</strong> {{popular_courses}}</small>
          </div>
        </a>
        {{/learning_paths}}
      </div>
    </div>
  </section>
  {{/learning_paths}}

  <!-- Program Types -->
  {{#program_types}}
  <section class="sc-section sc-section--soft" aria-labelledby="program-types-title">
    <div class="sc-container">
      <div class="sc-section-head sc-section-head--stack">
        <h2 id="program-types-title">Types de programmes</h2>
        <p class="sc-section-sub">Choisissez le format adapté</p>
      </div>
      <div class="sc-grid sc-grid--programs sc-stagger">
        {{#program_types}}
        <div class="sc-card">
          <div style="font-size: 2.5rem; margin-bottom: 1rem;">{{icon}}</div>
          <span class="sc-card-tag">{{level}}</span>
          <h3>{{title}}</h3>
          <p>{{description}}</p>
          <div class="sc-card-meta" style="margin-top: auto; padding-top: 1rem; border-top: 1px solid var(--sc-border);">
            <div><strong>Durée :</strong> {{duration}}</div>
            <div><strong>Résultat :</strong> {{outcomes}}</div>
            <div style="margin-top: 0.5rem;"><em>{{example}}</em></div>
          </div>
        </div>
        {{/program_types}}
      </div>
    </div>
  </section>
  {{/program_types}}

  <!-- Career Academy -->
  {{#career_academy}}
  <section class="sc-section" aria-labelledby="career-academy-title">
    <div class="sc-container">
      <div class="sc-section-head sc-section-head--stack">
        <h2 id="career-academy-title">{{title}}</h2>
        <p class="sc-section-sub">{{subtitle}}</p>
      </div>
      <div class="sc-grid sc-grid--features sc-stagger">
        {{#benefits}}
        <div class="sc-card"><p>✓ {{.}}</p></div>
        {{/benefits}}
      </div>
    </div>
  </section>
  {{/career_academy}}

  <!-- Why Campus -->
  {{#why_campus}}
  <section class="sc-section sc-section--soft" aria-labelledby="why-campus-title">
    <div class="sc-container">
      <div class="sc-section-head sc-section-head--stack">
        <h2 id="why-campus-title">Pourquoi nous choisir</h2>
        <p class="sc-section-sub">Des outils puissants</p>
      </div>
      <div class="sc-grid sc-grid--features sc-stagger">
        {{#why_campus}}
        <div class="sc-card">
          <div style="font-size: 2.5rem; margin-bottom: 1rem;">{{icon}}</div>
          <h3>{{title}}</h3>
          <p>{{description}}</p>
        </div>
        {{/why_campus}}
      </div>
    </div>
  </section>
  {{/why_campus}}

  <!-- University Use Cases -->
  {{#university_use_cases}}
  <section class="sc-section" aria-labelledby="use-cases-title">
    <div class="sc-container">
      <div class="sc-section-head sc-section-head--stack">
        <h2 id="use-cases-title">Comment les universités nous utilisent</h2>
        <p class="sc-section-sub">Cas d'usage concrets</p>
      </div>
      <div class="sc-grid sc-grid--programs sc-stagger">
        {{#university_use_cases}}
        <article class="sc-card">
          <div style="font-size: 2.5rem; margin-bottom: 1rem;">{{icon}}</div>
          <span class="sc-card-tag">Cas d'usage</span>
          <h3>{{title}}</h3>
          <p>{{description}}</p>
          <div class="sc-card-meta" style="margin-top: auto; padding-top: 1rem; border-top: 1px solid var(--sc-border);">
            <div><strong>Exemple :</strong> {{example}}</div>
            <div style="margin-top: 0.5rem;"><strong>Résultat :</strong> {{result}}</div>
          </div>
        </article>
        {{/university_use_cases}}
      </div>
    </div>
  </section>
  {{/university_use_cases}}

  <!-- High Quality Content -->
  {{#high_quality_content}}
  <section class="sc-section sc-section--soft" aria-labelledby="content-title">
    <div class="sc-container">
      <div class="sc-section-head sc-section-head--stack">
        <h2 id="content-title">{{title}}</h2>
        <p class="sc-section-sub">{{subtitle}}</p>
      </div>
      <div class="sc-grid sc-grid--features sc-stagger">
        {{#benefits}}
        <div class="sc-card"><p>✓ {{.}}</p></div>
        {{/benefits}}
      </div>
    </div>
  </section>
  {{/high_quality_content}}

  <!-- Government Training Features -->
  {{#government_training_features}}
  <section class="sc-section" aria-labelledby="training-features-title">
    <div class="sc-container">
      <div class="sc-section-head sc-section-head--stack">
        <h2 id="training-features-title">Formation gouvernementale</h2>
        <p class="sc-section-sub">Développez des compétences</p>
      </div>
      <div class="sc-grid sc-grid--features sc-stagger">
        {{#government_training_features}}
        <div class="sc-card">
          <div style="font-size: 2.5rem; margin-bottom: 1rem;">{{icon}}</div>
          <h3>{{title}}</h3>
          <p>{{description}}</p>
        </div>
        {{/government_training_features}}
      </div>
    </div>
  </section>
  {{/government_training_features}}

  <!-- Skills Tracks -->
  {{#skills_tracks}}
  <section class="sc-section sc-section--alt" aria-labelledby="skills-tracks-title">
    <div class="sc-container">
      <div class="sc-section-head sc-section-head--stack">
        <h2 id="skills-tracks-title">Skills Tracks adaptés</h2>
        <p class="sc-section-sub">Parcours ciblés</p>
      </div>
      <div class="sc-grid sc-grid--programs sc-stagger">
        {{#skills_tracks}}
        <a class="sc-card" href="{{{url}}}">
          <div style="font-size: 3rem; margin-bottom: 1rem;">{{icon}}</div>
          <h3>{{title}}</h3>
          <p style="font-weight: 600; color: var(--sc-accent); margin: 0.5rem 0;">{{subtitle}}</p>
          <p>{{description}}</p>
          <span class="sc-link" style="margin-top: auto; display: inline-block;">Explorer →</span>
        </a>
        {{/skills_tracks}}
      </div>
    </div>
  </section>
  {{/skills_tracks}}

  <!-- Government Solutions -->
  {{#government_solutions}}
  <section class="sc-section" aria-labelledby="gov-solutions-title">
    <div class="sc-container">
      <div class="sc-section-head sc-section-head--stack">
        <h2 id="gov-solutions-title">Solutions secteur public</h2>
        <p class="sc-section-sub">Transformez votre organisation</p>
      </div>
      <div class="sc-grid sc-grid--programs sc-stagger">
        {{#government_solutions}}
        <div class="sc-card">
          <div style="font-size: 2.5rem; margin-bottom: 1rem;">{{icon}}</div>
          <h3>{{title}}</h3>
          <p>{{description}}</p>
          <div class="sc-card-meta" style="margin-top: auto; padding-top: 1rem; border-top: 1px solid var(--sc-border);">
            {{#features}}
            <div>✓ {{.}}</div>
            {{/features}}
          </div>
        </div>
        {{/government_solutions}}
      </div>
    </div>
  </section>
  {{/government_solutions}}

  <!-- Catégories -->
  {{#hascategories}}
  <section class="sc-section" aria-labelledby="categories-title">
    <div class="sc-container">
      <div class="sc-section-head">
        <h2 id="categories-title">Explorez par catégorie</h2>
        <a class="sc-link" href="{{config.wwwroot}}/course/index.php">Voir toutes →</a>
      </div>
      <div class="sc-grid sc-grid--categories sc-stagger">
        {{#categories}}
        <a class="sc-card" href="{{{url}}}">
          {{#image}}
          <div class="sc-card-media">
            <img src="{{{image}}}" alt="{{name}}" loading="lazy">
          </div>
          {{/image}}
          <h3>{{name}}</h3>
          <p>{{meta}}</p>
        </a>
        {{/categories}}
      </div>
    </div>
  </section>
  {{/hascategories}}

  <!-- Cours -->
  {{#hascourses}}
  <section class="sc-section sc-section--soft" aria-labelledby="courses-title">
    <div class="sc-container">
      <div class="sc-section-head">
        <h2 id="courses-title">Cours populaires</h2>
        <a class="sc-link" href="{{config.wwwroot}}/course/search.php">Voir tous →</a>
      </div>
      <div class="sc-grid sc-grid--courses sc-stagger">
        {{#courses}}
        <article class="sc-card">
          {{#image}}
          <div class="sc-card-media">
            <img src="{{{image}}}" alt="{{title}}" loading="lazy">
          </div>
          {{/image}}
          <span class="sc-card-tag">Formation</span>
          <h3>{{title}}</h3>
          <p>{{summary}}</p>
          {{#canrequest}}
          <a class="sc-link" href="{{{enrolurl}}}" data-signup-modal>S'inscrire →</a>
          {{/canrequest}}
        </article>
        {{/courses}}
      </div>
    </div>
  </section>
  {{/hascourses}}

  <!-- Career Benefits -->
  {{#career_benefits}}
  <section class="sc-section" aria-labelledby="career-benefits-title">
    <div class="sc-container">
      <div class="sc-section-head sc-section-head--stack">
        <h2 id="career-benefits-title">Résultats carrière prouvés</h2>
        <p class="sc-section-sub">Les statistiques parlent</p>
      </div>
      <div class="sc-outcomes sc-stagger">
        {{#career_benefits}}
        <div class="sc-outcome">
          <div class="sc-outcome-value">{{stat}}</div>
          <div class="sc-outcome-label">{{title}}</div>
          <p style="font-size: 0.875rem; margin-top: 0.5rem; color: var(--sc-text-light);">{{description}}</p>
        </div>
        {{/career_benefits}}
      </div>
    </div>
  </section>
  {{/career_benefits}}

  <!-- Steps -->
  {{#hassteps}}
  <section class="sc-section sc-section--soft" aria-labelledby="steps-title">
    <div class="sc-container">
      <div class="sc-section-head sc-section-head--stack">
        <h2 id="steps-title">Comment ça marche</h2>
        <p class="sc-section-sub">Processus simple</p>
      </div>
      <div class="sc-steps sc-stagger">
        {{#steps}}
        <div class="sc-step">
          <div class="sc-step-number">{{number}}</div>
          <h3>{{title}}</h3>
          <p>{{text}}</p>
        </div>
        {{/steps}}
      </div>
    </div>
  </section>
  {{/hassteps}}

  <!-- Detailed Testimonials -->
  {{#detailed_testimonials}}
  <section class="sc-section" aria-labelledby="testimonials-title">
    <div class="sc-container">
      <div class="sc-section-head sc-section-head--stack">
        <h2 id="testimonials-title">Pourquoi nous choisir</h2>
        <p class="sc-section-sub">Histoires de transformation</p>
      </div>
      <div class="sc-grid sc-grid--testimonials sc-stagger">
        {{#detailed_testimonials}}
        <article class="sc-card">
          <p>"{{quote}}"</p>
          <div class="sc-card-meta" style="margin-top: auto; padding-top: 1rem; border-top: 1px solid var(--sc-border);">
            <div><strong>{{name}}</strong> - {{role}}</div>
            <div style="font-size: 0.875rem; color: var(--sc-text-light);">{{previous_role}}</div>
            <div style="margin-top: 0.5rem; font-weight: 600; color: var(--sc-primary);">{{outcome}}</div>
          </div>
        </article>
        {{/detailed_testimonials}}
      </div>
    </div>
  </section>
  {{/detailed_testimonials}}

  <!-- University Testimonials -->
  {{#university_testimonials}}
  <section class="sc-section" aria-labelledby="uni-testimonials-title">
    <div class="sc-container">
      <div class="sc-section-head sc-section-head--stack">
        <h2 id="uni-testimonials-title">Nos partenaires universitaires</h2>
        <p class="sc-section-sub">Témoignages de leaders</p>
      </div>
      <div class="sc-grid sc-grid--testimonials sc-stagger">
        {{#university_testimonials}}
        <article class="sc-card">
          <p>"{{quote}}"</p>
          <div class="sc-card-meta" style="margin-top: auto; padding-top: 1rem; border-top: 1px solid var(--sc-border);">
            <div><strong>{{name}}</strong></div>
            <div style="font-size: 0.875rem; color: var(--sc-text-light);">{{title}}</div>
            <div style="margin-top: 0.5rem; font-weight: 600; color: var(--sc-primary);">{{university}}</div>
          </div>
        </article>
        {{/university_testimonials}}
      </div>
    </div>
  </section>
  {{/university_testimonials}}

  <!-- Government Case Studies -->
  {{#government_case_studies}}
  <section class="sc-section sc-section--alt" aria-labelledby="gov-cases-title">
    <div class="sc-container">
      <div class="sc-section-head sc-section-head--stack">
        <h2 id="gov-cases-title">Études de cas</h2>
        <p class="sc-section-sub">Transformation secteur public</p>
      </div>
      <div class="sc-grid sc-grid--testimonials sc-stagger">
        {{#government_case_studies}}
        <article class="sc-card">
          <span class="sc-card-tag">Success Story</span>
          <h3>{{client}}</h3>
          <p><strong>Défi :</strong> {{challenge}}</p>
          <p><strong>Solution :</strong> {{solution}}</p>
          <div class="sc-card-meta" style="margin-top: auto; padding-top: 1rem; border-top: 1px solid var(--sc-border);">
            <strong>Résultats :</strong>
            {{#results}}
            <div>✓ {{.}}</div>
            {{/results}}
          </div>
        </article>
        {{/government_case_studies}}
      </div>
    </div>
  </section>
  {{/government_case_studies}}

  <!-- FAQ -->
  {{#hasfaq}}
  <section class="sc-section" aria-labelledby="faq-title">
    <div class="sc-container">
      <div class="sc-section-head sc-section-head--stack">
        <h2 id="faq-title">Questions fréquentes</h2>
        <p class="sc-section-sub">Trouvez vos réponses</p>
      </div>
      <div class="sc-faq sc-stagger">
        {{#faq}}
        <details class="sc-faq-item">
          <summary>{{question}}</summary>
          <div class="sc-faq-answer">{{answer}}</div>
        </details>
        {{/faq}}
      </div>
    </div>
  </section>
  {{/hasfaq}}

  <!-- CTA Final -->
  {{^hidecta}}
  <section class="sc-cta" aria-labelledby="cta-title">
    <div class="sc-container">
      <div class="sc-cta-inner">
        <div>
          <h2 id="cta-title">Prêt à commencer ?</h2>
          <p>Rejoignez des milliers d'apprenants et organisations</p>
        </div>
        <div class="sc-cta-actions">
          {{#cta_primary_label}}
          <a class="sc-btn sc-btn--primary" href="{{{cta_primary_url}}}">{{cta_primary_label}}</a>
          {{/cta_primary_label}}
          <a class="sc-btn sc-btn--ghost" href="{{{cta_secondary_url}}}">{{cta_secondary_label}}</a>
        </div>
      </div>
    </div>
  </section>
  {{/hidecta}}

  <!-- Footer -->
  {{> theme_spacechild/spacechild/signup_modal }}
  {{> theme_spacechild/spacechild/landing_footer }}

</div>
