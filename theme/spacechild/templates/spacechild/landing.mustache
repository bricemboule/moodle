<!-- ============================================
     LANDING PAGE HOME - STYLE COURSERA EXACT
     Navigation + Hero inspirés de coursera.org
     ============================================ -->

<div class="cs-landing cs-landing--full has-topstrip">

  <!-- Top Strip - Barre publics style Coursera -->
  <div class="sc-topbar">
    <div class="sc-container sc-topbar-inner">
      <span class="sc-topbar-label">Publics</span>
      <a href="{{peopleurl}}">Pour les personnes</a>
      <a href="{{universitiesurl}}">Pour les universités</a>
      <a href="{{governmentsurl}}">Pour les gouvernements</a>
    </div>
  </div>

  <!-- Navigation principale style Coursera -->
  <header class="sc-nav">
    <div class="sc-container sc-nav-inner">
      <!-- Logo -->
      <a class="sc-logo" href="{{wwwroot}}">
        <img class="sc-logo-image" src="{{wwwroot}}/theme/spacechild/images/LOGO.png" alt="{{sitename}}">
      </a>

      <!-- Lien Explorer -->
      <a class="sc-nav-link" href="{{exploreurl}}">Explorer</a>

      <!-- Barre de recherche avec icône -->
      <form class="sc-nav-search" method="get" action="{{searchurl}}" role="search">
        <label class="sc-visually-hidden" for="sc-nav-search">Que souhaitez-vous apprendre ?</label>
        <input
          id="sc-nav-search"
          type="text"
          name="search"
          placeholder="Que souhaitez-vous apprendre ?"
          aria-label="Que souhaitez-vous apprendre ?"
        >
        <button class="sc-search-btn" type="submit" aria-label="Rechercher">
          <svg class="sc-search-btn__icon" width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <path d="M9 17C13.4183 17 17 13.4183 17 9C17 4.58172 13.4183 1 9 1C4.58172 1 1 4.58172 1 9C1 13.4183 4.58172 17 9 17Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            <path d="M19 19L14.65 14.65" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </button>
      </form>

      <!-- Actions -->
      <nav class="sc-nav-actions" aria-label="Actions utilisateur">
        <a class="sc-link" href="{{loginurl}}" data-login-modal data-modal-title="Connexion">Connexion</a>
        <a class="sc-btn sc-btn--signup" href="{{signupurl}}" data-signup-modal data-modal-title="Inscription">S'inscrire gratuitement</a>
      </nav>
    </div>
  </header>

  <!-- Hero Section -->
  <section class="sc-hero" aria-labelledby="hero-title">
    <div class="sc-container">
      <div class="sc-hero-grid">
        <div class="sc-hero-copy">
          <h1 id="hero-title">Apprenez sans limites</h1>
          <p>
            Démarrez, changez ou faites progresser votre carrière avec plus de 7 000 cours 
            issus d'universités et d'entreprises leaders mondiales.
          </p>

          <div class="sc-hero-actions">
            <a class="sc-btn sc-btn--primary" href="{{signupurl}}" data-signup-modal>
              Inscription gratuite
            </a>
          </div>

          <!-- Tags populaires -->
          <div class="sc-hero-tags">
            <span>ISO 9001</span>
            <span>Data Analytics</span>
            <span>Cybersécurité</span>
            <span>Management</span>
            <span>UX Design</span>
          </div>
        </div>

        <!-- Panel visuel -->
        <div class="sc-hero-panel">
          <div class="sc-feature-card sc-animate">
            <span class="sc-card-tag">Nouveau</span>
            <h3>Certificats Professionnels</h3>
            <p>Obtenez des compétences recherchées par les employeurs en 3-6 mois</p>
            <div class="sc-feature-meta">
              <span>Google</span>
              <span>IBM</span>
              <span>Microsoft</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Section Partenaires -->
  <section class="sc-section" aria-label="Nos partenaires">
    <div class="sc-container">
      <div class="sc-logos">
        <p class="sc-logos-title">Apprenez avec les meilleurs</p>
        <div class="sc-logos-row">
          <span class="sc-logo-pill">Google</span>
          <span class="sc-logo-pill">IBM</span>
          <span class="sc-logo-pill">Microsoft</span>
          <span class="sc-logo-pill">Stanford</span>
          <span class="sc-logo-pill">Yale</span>
          <span class="sc-logo-pill">Meta</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Section KPIs -->
  <section class="sc-section sc-section--soft" aria-labelledby="metrics-title">
    <div class="sc-container">
      <div class="sc-section-head sc-section-head--stack">
        <h2 id="metrics-title">Une plateforme qui fait la différence</h2>
        <p class="sc-section-sub">
          Des résultats concrets pour des milliers d'apprenants dans le monde
        </p>
      </div>

      <div class="sc-kpis sc-stagger">
        <div class="sc-kpi">
          <div class="sc-kpi-value">7 000+</div>
          <div class="sc-kpi-label">Cours disponibles</div>
        </div>
        <div class="sc-kpi">
          <div class="sc-kpi-value">350+</div>
          <div class="sc-kpi-label">Universités partenaires</div>
        </div>
        <div class="sc-kpi">
          <div class="sc-kpi-value">91%</div>
          <div class="sc-kpi-label">Bénéfices carrière positifs</div>
        </div>
        <div class="sc-kpi">
          <div class="sc-kpi-value">145M+</div>
          <div class="sc-kpi-label">Apprenants dans le monde</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Section Catégories -->
  {{#hascategories}}
  <section class="sc-section" aria-labelledby="categories-title">
    <div class="sc-container">
      <div class="sc-section-head">
        <h2 id="categories-title">Explorez par catégorie</h2>
        <a class="sc-link" href="{{exploreurl}}">
          Voir toutes les catégories →
        </a>
      </div>

      <div class="sc-grid sc-grid--categories sc-stagger">
        {{#categories}}
          <a class="sc-card" href="{{{url}}}">
            {{#image}}
              <div class="sc-card-media">
                <img src="{{{image}}}" alt="Catégorie {{name}}" loading="lazy">
              </div>
            {{/image}}
            <h3>{{name}}</h3>
            <p>{{meta}}</p>
          </a>
        {{/categories}}
      </div>
    </div>
  </section>
  {{/hascategories}}

  <!-- Section Objectifs -->
  <section class="sc-section sc-section--alt" aria-labelledby="goals-title">
    <div class="sc-container">
      <div class="sc-section-head sc-section-head--stack">
        <h2 id="goals-title">Qu'est-ce qui vous amène ici aujourd'hui ?</h2>
        <p class="sc-section-sub">Choisissez votre objectif</p>
      </div>

      <div class="sc-grid sc-grid--features sc-stagger">
        <div class="sc-card">
          <div style="font-size: 2.5rem; margin-bottom: 1rem;">🚀</div>
          <h3>Démarrer ma carrière</h3>
          <p>Lancez-vous dans un nouveau domaine professionnel avec des formations job-ready</p>
        </div>
        <div class="sc-card">
          <div style="font-size: 2.5rem; margin-bottom: 1rem;">🔄</div>
          <h3>Changer de carrière</h3>
          <p>Reconvertissez-vous dans un secteur porteur avec des certificats reconnus</p>
        </div>
        <div class="sc-card">
          <div style="font-size: 2.5rem; margin-bottom: 1rem;">📈</div>
          <h3>Progresser dans mon rôle</h3>
          <p>Développez de nouvelles compétences pour évoluer professionnellement</p>
        </div>
        <div class="sc-card">
          <div style="font-size: 2.5rem; margin-bottom: 1rem;">🎓</div>
          <h3>Explorer de nouveaux sujets</h3>
          <p>Apprenez pour le plaisir d'apprendre et enrichissez vos connaissances</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Section Cours populaires -->
  {{#hascourses}}
  <section class="sc-section" aria-labelledby="courses-title">
    <div class="sc-container">
      <div class="sc-section-head">
        <h2 id="courses-title">Cours populaires</h2>
        <a class="sc-link" href="{{searchurl}}">
          Voir tous les cours →
        </a>
      </div>

      <div class="sc-grid sc-grid--courses sc-stagger">
        {{#courses}}
          <article class="sc-card">
            {{#image}}
              <div class="sc-card-media">
                <img src="{{{image}}}" alt="{{title}}" loading="lazy">
              </div>
            {{/image}}
            <span class="sc-card-tag">Formation</span>
            <h3>{{title}}</h3>
            <p>{{summary}}</p>
            {{#canrequest}}
              <a class="sc-link" href="{{{enrolurl}}}" data-signup-modal>
                S'inscrire →
              </a>
            {{/canrequest}}
          </article>
        {{/courses}}
      </div>
    </div>
  </section>
  {{/hascourses}}

  <!-- Section Avantages -->
  <section class="sc-section sc-section--soft" aria-labelledby="benefits-title">
    <div class="sc-container">
      <div class="sc-section-head sc-section-head--stack">
        <h2 id="benefits-title">Pourquoi nous choisir</h2>
        <p class="sc-section-sub">
          Une expérience d'apprentissage pensée pour votre réussite
        </p>
      </div>

      <div class="sc-grid sc-grid--features sc-stagger">
        <div class="sc-card">
          <span class="sc-card-tag">Qualité</span>
          <h3>Formateurs experts</h3>
          <p>Apprenez avec des professionnels reconnus qui partagent leur expérience terrain</p>
        </div>

        <div class="sc-card">
          <span class="sc-card-tag">Pratique</span>
          <h3>Projets concrets</h3>
          <p>Développez votre portfolio avec des cas réels et des exercices guidés</p>
        </div>

        <div class="sc-card">
          <span class="sc-card-tag">Flexibilité</span>
          <h3>À votre rythme</h3>
          <p>Accédez aux cours 24/7 depuis n'importe quel appareil</p>
        </div>

        <div class="sc-card">
          <span class="sc-card-tag">Certification</span>
          <h3>Attestations reconnues</h3>
          <p>Valorisez vos compétences avec des certificats partageables</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Section Témoignages -->
  {{#hastestimonials}}
  <section class="sc-section" aria-labelledby="testimonials-title">
    <div class="sc-container">
      <div class="sc-section-head sc-section-head--stack">
        <h2 id="testimonials-title">Ce qu'en disent nos apprenants</h2>
        <p class="sc-section-sub">
          Des milliers de personnes ont déjà transformé leur carrière
        </p>
      </div>

      <div class="sc-grid sc-grid--testimonials sc-stagger">
        {{#testimonials}}
          <article class="sc-card sc-card--testimonial">
            <p>"{{quote}}"</p>
            <div class="sc-testimonial-by">
              <strong>{{name}}</strong> • {{role}}
            </div>
          </article>
        {{/testimonials}}
      </div>
    </div>
  </section>
  {{/hastestimonials}}

  <!-- Section Comment ça marche -->
  <section class="sc-section sc-section--alt" aria-labelledby="steps-title">
    <div class="sc-container">
      <div class="sc-section-head sc-section-head--stack">
        <h2 id="steps-title">Commencez en 3 étapes simples</h2>
        <p class="sc-section-sub">
          Un parcours d'apprentissage clair pour atteindre vos objectifs
        </p>
      </div>

      <div class="sc-steps sc-stagger">
        <div class="sc-step">
          <div class="sc-step-number">01</div>
          <h3>Choisissez votre formation</h3>
          <p>Explorez notre catalogue et trouvez le cours qui correspond à vos besoins</p>
        </div>

        <div class="sc-step">
          <div class="sc-step-number">02</div>
          <h3>Apprenez à votre rythme</h3>
          <p>Suivez les modules vidéo, réalisez les exercices et les projets pratiques</p>
        </div>

        <div class="sc-step">
          <div class="sc-step-number">03</div>
          <h3>Obtenez votre certification</h3>
          <p>Validez vos acquis et recevez votre attestation officielle</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Section FAQ -->
  {{#hasfaq}}
  <section class="sc-section" aria-labelledby="faq-title">
    <div class="sc-container">
      <div class="sc-section-head sc-section-head--stack">
        <h2 id="faq-title">Questions fréquentes</h2>
        <p class="sc-section-sub">
          Tout ce que vous devez savoir sur nos formations
        </p>
      </div>

      <div class="sc-faq sc-stagger">
        {{#faq}}
          <details class="sc-faq-item">
            <summary>{{question}}</summary>
            <div class="sc-faq-answer">{{answer}}</div>
          </details>
        {{/faq}}
      </div>
    </div>
  </section>
  {{/hasfaq}}

  <!-- CTA Final -->
  <section class="sc-cta" aria-labelledby="cta-title">
    <div class="sc-container">
      <div class="sc-cta-inner">
        <div>
          <h2 id="cta-title">Prêt à commencer votre formation ?</h2>
          <p>Rejoignez des milliers d'apprenants et accédez à plus de 7 000 cours dès aujourd'hui</p>
        </div>
        <div class="sc-cta-actions">
          <a class="sc-btn sc-btn--primary" href="{{signupurl}}" data-signup-modal>
            Créer un compte gratuit
          </a>
          <a class="sc-btn sc-btn--ghost" href="{{exploreurl}}">
            Explorer le catalogue
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Footer -->
  {{> theme_spacechild/spacechild/signup_modal }}
  {{> theme_spacechild/spacechild/landing_footer }}

</div>
